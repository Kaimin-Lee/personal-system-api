server:
  port: 40910

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/personal_system?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  mail:
    host: smtp.qq.com
    port: 465
    username: 1585037731@qq.com # 你的发件邮箱
    password: frewpwwolyprihec    # 你的邮箱授权码（不是登录密码）
    protocol: smtps
    properties:
      mail:
        smtp:
          ssl:
            enable: true

mybatis-plus:
  # ?? MyBatis-Plus ?? xml ???????
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    # ????? SQL ?????????????????
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true # ???????? (? user_id ???? userId)